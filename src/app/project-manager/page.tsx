import { Container, Text } from '@mantine/core';

const ProjectManagerDashboard = () => {
  return (
    <Container
      ff={'poppins'}
      className="bg-white h-screen rounded-tr-[50px] rounded-tl-[50px] p-10"
      fluid
    >
      <Text>This is pm dashboard</Text>
    </Container>
  );
};

export default ProjectManagerDashboard;
